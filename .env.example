# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bankcards
DB_USERNAME=postgres
DB_PASSWORD=your_secure_password

# JWT
JWT_SECRET=your_base64_encoded_secret_key_at_least_32_bytes
JWT_EXPIRATION=86400000

# AES encryption (card numbers)
AES_KEY=your_base64_encoded_aes_key_at_least_16_bytes
